version: 2

models:

      - name: dw_dim_tickers
        description: This model ingests staging data from the tickers_snapshot model.

      - name: dw_fact_prices
        description: This model incrementally ingests staging data from stg_sk_prices model.

      - name: dw_fact_transactions
        description: This model ingests staging data from stg_sk_transactions model.