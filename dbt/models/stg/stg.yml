version: 2

models:
  - name: stg_prices
    description: This model creates the pk (date + ticker) to run tests on
    columns:
      - name: pk
        description: Primary key
        tests:
          - unique
          - not_null