version: 2

models:
  - name: stg_prices
    description: This model creates the pk to run tests on
    columns:
      - name: pk
        description: Primary key
        tests:
          - unique
          - not_null